# Optional. The deterministic resistant/vulnerable demo models need no API key.
# Add this value to a local .env file to test a real OpenAI model.
OPENAI_API_KEY=

# Current default for behavioral tests. Pin a snapshot for durable evidence.
SKILLTRUST_OPENAI_MODEL=gpt-5.6-terra
